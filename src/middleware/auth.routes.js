// import { Router } from "express";

// import { refresh,logout, LoginUserController } from "../user/user.controllers.js";
// import { routes } from "../utils/routes.js";

// export const authrouter = Router();

// authrouter.post("/login", LoginUserController, refresh);
// authrouter.post("/refresh", refresh);
// authrouter.post("/logout", logout);

// export const Authrouter = routes.use("/auth", authrouter);